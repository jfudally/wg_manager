# Base URL of the wg-manager FastAPI control plane. Must include the
# scheme; do not append a trailing slash. The Next.js dashboard reads this
# at build and runtime via NEXT_PUBLIC_ (so it's safe in client bundles).
NEXT_PUBLIC_WG_MANAGER_API=http://127.0.0.1:8000
